[user]
	email = “emond.jesse@gmail.com”
	name = JesseEmond
[push]
	default = simple
[core]
	autocrlf = false
	filemode = false
	excludesfile = /home/dysleixa/.gitignore
[color]
	ui = true
[alias]
	st   = status
	br   = branch
        ph   = push
        pl   = pull
        co   = checkout
        c    = commit --verbose
        cm   = commit -m 
        mg   = merge
        a    = add
        aa   = add --all
        tree = log --graph --decorate --pretty=oneline --abbrev-commit
        tr   = log --graph --decorate --pretty=oneline --abbrev-commit
        d    = diff
        dc   = diff --cached
	conflicts = diff --name-only --diff-filter=U
        lg   = log
